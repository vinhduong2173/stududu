@AGENTS.md

<!-- Ghi chú riêng cho Claude Code (không áp dụng cho Antigravity) -->
- Khi chạy lệnh build/test, dùng đúng package manager đang có trong repo (kiểm tra lockfile trước: package-lock.json/yarn.lock/pnpm-lock.yaml).
- Nếu cần sửa file ngoài phạm vi task đang làm, hỏi lại trước khi sửa thay vì tự tiện mở rộng.
