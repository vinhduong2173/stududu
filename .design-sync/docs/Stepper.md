---
category: Foundation
---
Thanh bước cho luồng onboarding nhiều bước. `steps` là mảng nhãn, `currentStep` đếm từ 1; bước đã qua tô màu primary.

```tsx
<Stepper steps={["Ngôn ngữ", "Sở thích", "Hoàn thiện"]} currentStep={2} />
```
