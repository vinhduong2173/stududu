import * as React from "react";
import { BlockDialog } from "frontend";

export function XacNhanChan() {
  return (
    <BlockDialog
      open
      onClose={() => {}}
      targetId={2}
      targetName="Sarah Jenkins"
    />
  );
}
