import * as React from "react";
import { EndorseModal } from "frontend";

export function GhiNhan() {
  return (
    <EndorseModal
      open
      onClose={() => {}}
      targetId={2}
      targetName="Sarah Jenkins"
    />
  );
}
